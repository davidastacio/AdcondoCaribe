export function formatIncidentDate(value:string){const date=new Date(value);const day=String(date.getDate()).padStart(2,"0");const month=String(date.getMonth()+1).padStart(2,"0");return `${day}/${month}/${date.getFullYear()}`}
export function formatIncidentDateTime(value:string){const date=new Date(value);let hour=date.getHours();const minutes=String(date.getMinutes()).padStart(2,"0");const period=hour>=12?"p. m.":"a. m.";hour=hour%12||12;return `${formatIncidentDate(value)}, ${hour}:${minutes} ${period}`}
export function formatTimelineDate(value:string){const date=new Date(value);const months=["ene","feb","mar","abr","may","jun","jul","ago","sep","oct","nov","dic"];let hour=date.getHours();const minutes=String(date.getMinutes()).padStart(2,"0");const period=hour>=12?"p. m.":"a. m.";hour=hour%12||12;return `${date.getDate()} ${months[date.getMonth()]}, ${hour}:${minutes} ${period}`}
