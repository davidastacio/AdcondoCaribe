"use client";import { MaterialRequestCard } from "@/components/inventory/request-card";import { listRequests } from "@/features/inventory/service";import { ClipboardPlus } from "lucide-react";import Link from "next/link";import { useEffect,useState } from "react";import type { MaterialRequest } from "@/features/inventory/types";
export default function RequestsPage(){const [requests,setRequests]=useState<MaterialRequest[]>([]);useEffect(()=>setRequests(listRequests()),[]);return <><header className="module-heading"><div><span className="eyebrow"><ClipboardPlus/> Abastecimiento</span><h1>Mis solicitudes</h1><p>Consulta el progreso de los materiales solicitados para tus torres.</p></div><Link className="btn btn--primary" href="/supervisor/solicitudes/nueva">Nueva solicitud</Link></header><div className="request-card-grid">{requests.map(r=><MaterialRequestCard key={r.id} request={r} basePath="/supervisor/solicitudes"/>)}</div></>}
