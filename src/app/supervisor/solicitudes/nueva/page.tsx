"use client";import { MaterialRequestForm } from "@/components/inventory/material-request-form";import { ArrowLeft } from "lucide-react";import Link from "next/link";import { useSearchParams } from "next/navigation";import { Suspense } from "react";
function Form(){const q=useSearchParams();return <MaterialRequestForm initialTowerId={q.get("towerId")??undefined} initialItemIds={(q.get("items")??"").split(",").filter(Boolean)}/>}
export default function NewRequestPage(){return <><header className="module-heading compact"><div><Link className="back-link" href="/supervisor/solicitudes"><ArrowLeft/> Mis solicitudes</Link><h1>Nueva solicitud de materiales</h1><p>Incluye varios materiales en una sola solicitud.</p></div></header><Suspense fallback={<div className="card">Preparando formulario...</div>}><Form/></Suspense></>}
