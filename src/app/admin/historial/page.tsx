"use client";import {ActivityTimeline} from "@/components/reports/report-ui";import {listActivity} from "@/features/reports/service";import {listTowers} from "@/features/towers/service";import {History} from "lucide-react";import {useState} from "react";export default function HistoryPage(){const all=listActivity(),[user,setUser]=useState(""),[tower,setTower]=useState(""),[entity,setEntity]=useState(""),shown=all.filter(a=>(!user||a.userName===user)&&(!tower||a.towerId===tower)&&(!entity||a.entityType===entity));return <><header className="module-heading"><div><span className="eyebrow"><History/> Auditoría</span><h1>Historial general</h1><p>Registro cronológico de acciones importantes en la plataforma.</p></div></header><section className="report-filters card"><select value={user} onChange={e=>setUser(e.target.value)}><option value="">Todos los usuarios</option>{[...new Set(all.map(a=>a.userName))].map(x=><option key={x}>{x}</option>)}</select><select value={tower} onChange={e=>setTower(e.target.value)}><option value="">Todas las torres</option>{listTowers().map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><select value={entity} onChange={e=>setEntity(e.target.value)}><option value="">Todas las entidades</option>{["VISIT","INCIDENT","INVENTORY","REQUEST","USER","DOCUMENT"].map(x=><option key={x}>{x}</option>)}</select><input type="date"/></section><section className="card report-activity"><ActivityTimeline items={shown}/></section></>}
