"use client";import {UserForm} from "@/components/users/user-form";import {getUser,updateUser} from "@/features/users/service";import {ArrowLeft} from "lucide-react";import Link from "next/link";import {useParams,useRouter} from "next/navigation";export default function Page(){const {userId}=useParams<{userId:string}>(),router=useRouter(),u=getUser(userId);return <><header className="module-heading compact"><div><Link className="back-link" href={`/admin/usuarios/${userId}`}><ArrowLeft/> {u.firstName} {u.lastName}</Link><h1>Editar usuario</h1></div></header><UserForm user={u} onSubmit={v=>{updateUser(userId,v);router.push(`/admin/usuarios/${userId}`)}}/></>}
