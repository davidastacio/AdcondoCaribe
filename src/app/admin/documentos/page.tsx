"use client";import {DocumentCard} from "@/components/reports/report-ui";import {DocumentUploader} from "@/components/reports/document-uploader";import {listDocuments} from "@/features/reports/service";import {FileText} from "lucide-react";import {useState} from "react";export default function Documents(){const [tick,setTick]=useState(0),docs=listDocuments();return <><header className="module-heading"><div><span className="eyebrow"><FileText/> Archivo central</span><h1>Documentos</h1><p>Documentación relacionada con las torres residenciales.</p></div></header><div className="documents-layout"><DocumentUploader onDone={()=>setTick(tick+1)}/><section className="document-list">{docs.map(d=><DocumentCard key={d.id} document={d} onChange={()=>setTick(tick+1)}/>)}</section></div></>}
