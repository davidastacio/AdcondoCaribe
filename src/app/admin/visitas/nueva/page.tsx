"use client";import {VisitForm} from "@/components/visits/visit-form";import type {VisitInput} from "@/features/visits/types";import {ArrowLeft} from "lucide-react";import Link from "next/link";import {useRouter,useSearchParams} from "next/navigation";import {Suspense} from "react";
function Form(){const q=useSearchParams(),router=useRouter();async function create(value:VisitInput){const response=await fetch("/api/admin/visits",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(value)}),data=await response.json() as {visit?:{id:string};error?:string};if(!response.ok||!data.visit)throw new Error(data.error??"No se pudo programar la visita.");router.push("/admin/visitas");router.refresh()}return <VisitForm initialTowerId={q.get("towerId")??undefined} initialSupervisorId={q.get("supervisorId")??undefined} onSubmit={create}/>}
export default function Page(){return <><header className="module-heading compact"><div><Link className="back-link" href="/admin/visitas"><ArrowLeft/> Visitas</Link><h1>Programar visita</h1><p>Asigna una torre real, supervisor, fecha y hora.</p></div></header><Suspense fallback={<div className="card">Preparando formulario...</div>}><Form/></Suspense></>}
