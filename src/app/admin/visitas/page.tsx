"use client";import {listVisits} from "@/features/visits/service";import {VisitStatusBadge} from "@/components/visits/visit-status-badge";import {VisitFilters,type VisitFilterState} from "@/components/visits/visit-filters";import {CalendarDays,Plus} from "lucide-react";import Link from "next/link";import {useState} from "react";export default function AdminVisits(){const all=listVisits(),[f,setF]=useState<VisitFilterState>({q:"",supervisor:"",tower:"",status:"",type:""}),shown=all.filter(v=>(!f.q||(v.code+v.towerName).toLowerCase().includes(f.q.toLowerCase()))&&(!f.supervisor||v.supervisor===f.supervisor)&&(!f.tower||v.towerName===f.tower)&&(!f.status||v.status===f.status)&&(!f.type||v.visitType===f.type));return <><header className="module-heading"><div><span className="eyebrow"><CalendarDays/> Operación</span><h1>Visitas programadas</h1><p>Consulta y administra todas las supervisiones residenciales.</p></div><div className="heading-actions"><Link className="btn btn--soft" href="/admin/calendario">Ver calendario</Link><Link className="btn btn--primary" href="/admin/visitas/nueva"><Plus/> Programar visita</Link></div></header><VisitFilters value={f} onChange={setF} supervisors={[...new Set(all.map(v=>v.supervisor))]} towers={[...new Set(all.map(v=>v.towerName))]} types={[...new Set(all.map(v=>v.visitType??''))]}/><div className="admin-visit-list">{shown.map(v=><Link href={`/admin/visitas/${v.id}`} key={v.id}><b>{v.code}</b><span><strong>{v.towerName}</strong><small>{v.supervisor}</small></span><time>{v.scheduledDate}<small>{v.scheduledTime}</small></time><span>{v.visitType}</span><VisitStatusBadge status={v.status}/><em>{v.progress}%</em></Link>)}</div></>}
