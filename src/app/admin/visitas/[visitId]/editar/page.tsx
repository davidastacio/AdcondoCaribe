"use client";import {VisitForm} from "@/components/visits/visit-form";import {getVisitRecord,updateVisit} from "@/features/visits/service";import {ArrowLeft} from "lucide-react";import Link from "next/link";import {useParams,useRouter} from "next/navigation";export default function Page(){const {visitId}=useParams<{visitId:string}>(),router=useRouter(),v=getVisitRecord(visitId);return <><header className="module-heading compact"><div><Link className="back-link" href={`/admin/visitas/${visitId}`}><ArrowLeft/> {v.code}</Link><h1>Editar visita</h1></div></header><VisitForm visit={v} onSubmit={x=>{updateVisit(visitId,x);router.push(`/admin/visitas/${visitId}`)}}/></>}
