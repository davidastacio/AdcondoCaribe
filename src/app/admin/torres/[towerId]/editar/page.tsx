"use client";import {TowerForm} from "@/components/towers/tower-form";import {getTower,updateTower} from "@/features/towers/service";import type {Tower} from "@/features/towers/types";import {ArrowLeft} from "lucide-react";import Link from "next/link";import {useParams,useRouter} from "next/navigation";import {useEffect,useState} from "react";
export default function EditTower(){const {towerId}=useParams<{towerId:string}>(),router=useRouter(),[tower,setTower]=useState<Tower>();useEffect(()=>setTower(getTower(towerId)),[towerId]);if(!tower)return <div className="card">Cargando...</div>;return <><header className="module-heading compact"><div><Link className="back-link" href={`/admin/torres/${towerId}`}><ArrowLeft/> {tower.name}</Link><h1>Editar torre</h1><p>El identificador interno está protegido.</p></div></header><TowerForm tower={tower} onSubmit={input=>{updateTower(towerId,input);router.push(`/admin/torres/${towerId}`)}}/></>}
