"use client";import { InventoryStatusBadge } from "@/components/inventory/badges";import { listInventory } from "@/features/inventory/service";import type { TowerInventory } from "@/features/inventory/types";import { AlertTriangle,Boxes,PackageCheck,PackageX,Search } from "lucide-react";import { useEffect,useMemo,useState } from "react";
export default function AdminInventory(){const [items,setItems]=useState<TowerInventory[]>([]);const [query,setQuery]=useState("");const [status,setStatus]=useState("ALL");useEffect(()=>setItems(listInventory()),[]);const filtered=useMemo(()=>items.filter(i=>(status==="ALL"||i.status===status)&&(i.itemName+i.towerName+i.sector).toLowerCase().includes(query.toLowerCase())),[items,query,status]);const count=(s:string)=>items.filter(i=>i.status===s).length;return <><header className="module-heading"><div><span className="eyebrow"><Boxes/> Vista consolidada</span><h1>Inventario general</h1><p>Existencias registradas en todas las torres residenciales.</p></div></header><div className="inventory-kpis"><article><Boxes/><span><b>{items.length}</b>Registros</span></article><article><PackageCheck/><span><b>{count("AVAILABLE")}</b>Disponibles</span></article><article><AlertTriangle/><span><b>{count("LOW_STOCK")}</b>Stock bajo</span></article><article><PackageX/><span><b>{count("OUT_OF_STOCK")}</b>Agotados</span></article></div><section className="card inventory-table-card"><div className="inventory-filters"><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar material o torre..."/></label><select value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">Todos los estados</option><option value="AVAILABLE">Disponible</option><option value="LOW_STOCK">Bajo stock</option><option value="OUT_OF_STOCK">Agotado</option><option value="NOT_VERIFIED">No verificado</option></select></div><div className="responsive-table"><table><thead><tr><th>Material</th><th>Torre residencial</th><th>Cantidad</th><th>Ubicación</th><th>Estado</th><th>Última revisión</th></tr></thead><tbody>{filtered.map(i=><tr key={i.id}><td><b>{i.itemName}</b><small>{i.category}</small></td><td>{i.towerName}<small>{i.sector}</small></td><td>{i.quantity} {i.unit}</td><td>{i.location}</td><td><InventoryStatusBadge status={i.status}/></td><td>{i.lastCheckedAt?new Date(i.lastCheckedAt).toLocaleDateString("es-DO"):"Pendiente"}</td></tr>)}</tbody></table></div></section></>}
