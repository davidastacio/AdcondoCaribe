import type {Tower} from "@/features/towers/types";import {Building2,CalendarDays,ChevronRight,MapPin,ShieldAlert,Users} from "lucide-react";import Link from "next/link";import {TowerStatusBadge} from "./tower-status-badge";
export function TowerCard({tower,role="admin",openIncidents=0}:{tower:Tower;role?:"admin"|"supervisor";openIncidents?:number}){return <article className="tower-card"><header><span><Building2/></span><div><small>{tower.code}</small><h2>{tower.name}</h2><p><MapPin/>{tower.sector}</p></div><TowerStatusBadge status={tower.status}/></header><p className="tower-address">{tower.address}</p><div className="tower-card-data"><span><Users/><b>{tower.supervisors.join(", ")}</b>Supervisión</span><span><CalendarDays/><b>{tower.nextVisit??"Sin programar"}</b>Próxima visita</span><span><ShieldAlert/><b>{openIncidents}</b>Incidencias abiertas</span></div><Link href={`/${role}/torres/${tower.id}`}>Ver torre <ChevronRight/></Link></article>}
