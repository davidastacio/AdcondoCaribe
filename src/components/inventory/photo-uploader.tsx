"use client";
import type { InventoryPhoto } from "@/features/inventory/types";
import { Camera, ImagePlus, Trash2 } from "lucide-react";
import { useRef } from "react";
export function InventoryPhotoUploader({photos,onChange}:{photos:InventoryPhoto[];onChange:(photos:InventoryPhoto[])=>void}){const camera=useRef<HTMLInputElement>(null);const upload=useRef<HTMLInputElement>(null);const add=(files:FileList|null)=>{if(!files)return;onChange([...photos,...Array.from(files).map(file=>({id:crypto.randomUUID(),url:URL.createObjectURL(file),createdAt:new Date().toISOString()}))])};return <div className="inventory-photo-uploader"><div><button type="button" onClick={()=>camera.current?.click()}><Camera/>Tomar foto</button><button type="button" onClick={()=>upload.current?.click()}><ImagePlus/>Subir foto</button></div><input ref={camera} hidden type="file" accept="image/*" capture="environment" onChange={e=>add(e.target.files)}/><input ref={upload} hidden type="file" accept="image/*" onChange={e=>add(e.target.files)}/>{photos.length>0&&<section>{photos.map(photo=><article key={photo.id} style={{backgroundImage:`url(${photo.url})`}}><button type="button" onClick={()=>onChange(photos.filter(p=>p.id!==photo.id))}><Trash2/></button></article>)}</section>}</div>}
