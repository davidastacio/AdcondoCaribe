import {Search} from "lucide-react";export interface VisitFilterState{q:string;supervisor:string;tower:string;status:string;type:string}export function VisitFilters({value,onChange,supervisors,towers,types}:{value:VisitFilterState;onChange:(v:VisitFilterState)=>void;supervisors:string[];towers:string[];types:string[]}){const set=(k:keyof VisitFilterState,v:string)=>onChange({...value,[k]:v});return <div className="visit-admin-filters card"><label><Search/><input value={value.q} onChange={e=>set('q',e.target.value)} placeholder="Buscar código o torre..."/></label>{[['supervisor','Todos los supervisores',supervisors],['tower','Todas las torres',towers],['status','Todos los estados',['SCHEDULED','IN_PROGRESS','COMPLETED','RESCHEDULED','CANCELLED']],['type','Todos los tipos',types]].map(([k,l,opts])=><select key={String(k)} value={value[k as keyof VisitFilterState]} onChange={e=>set(k as keyof VisitFilterState,e.target.value)}><option value="">{String(l)}</option>{(opts as string[]).map(o=><option key={o}>{o}</option>)}</select>)}</div>}
