"use client";import {cancelVisit,rescheduleVisit} from "@/features/visits/service";import type {Visit} from "@/features/visits/types";import {X} from "lucide-react";import {useState} from "react";export function RescheduleVisitModal({visit,onClose,onDone}:{visit:Visit;onClose:()=>void;onDone:()=>void}){const [date,setDate]=useState(visit.scheduledDate),[time,setTime]=useState(visit.scheduledTime),[reason,setReason]=useState("");return <div className="visit-modal"><section><button onClick={onClose}><X/></button><h2>Reprogramar visita</h2><label>Nueva fecha<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Nueva hora<input type="time" value={time} onChange={e=>setTime(e.target.value)}/></label><label>Motivo *<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><button className="btn btn--primary" disabled={!reason.trim()} onClick={()=>{rescheduleVisit(visit.id,date,time,reason);onDone()}}>Confirmar reprogramación</button></section></div>}export function CancelVisitModal({visit,onClose,onDone}:{visit:Visit;onClose:()=>void;onDone:()=>void}){const [reason,setReason]=useState("");return <div className="visit-modal"><section><button onClick={onClose}><X/></button><h2>Cancelar visita</h2><p>La visita no se eliminará y conservará todo su historial.</p><label>Motivo obligatorio<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><button className="btn btn--danger" disabled={!reason.trim()} onClick={()=>{cancelVisit(visit.id,reason);onDone()}}>Cancelar visita</button></section></div>}
