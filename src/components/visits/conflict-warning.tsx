import type {Visit} from "@/features/visits/types";import {AlertTriangle} from "lucide-react";export function ConflictWarning({visit}:{visit:Visit}){return <div className="conflict-warning"><AlertTriangle/><div><b>Conflicto de horario</b><p>{visit.supervisor} ya tiene una visita en {visit.towerName} de {visit.scheduledTime} durante {visit.estimatedDuration??60} minutos.</p><small>Elige otro horario para continuar.</small></div></div>}
